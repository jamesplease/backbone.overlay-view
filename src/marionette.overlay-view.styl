.overlay-view
  position fixed
  top 0
  left 0
  width 100%
  height 100%
  // < 10k zIndex is for things below the overlay.
  // > 10k zIndex is for things above the overlay.
  // My recommendation is to use increments of 1000 to keep it
  // relatively orderly.
  z-index 10000

  &.overlay-view-hide
    display none